# Base URL used by the HTTP client. In development the Vite dev server
# proxies /api to http://localhost:8000, so the default value below
# works without change. In production, point this at the public API origin
# (or leave empty to use same-origin relative URLs behind a reverse proxy).
VITE_API_BASE_URL=
